


<!DOCTYPE HTML>
<html lang="en">


<!-- Mirrored from wp.alithemes.com/html/evara/evara-backend/index.html by HTTrack Website Copier/3.x [XR&CO'2014], Sun, 01 Aug 2021 15:32:35 GMT -->
<head>
    <meta charset="utf-8">
    <title>ZoneApple-admin</title>
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta property="og:title" content="">
    <meta property="og:type" content="">
    <meta property="og:url" content="">
    <meta property="og:image" content="">
    <!-- Favicon -->
    <link rel="shortcut icon" type="image/x-icon" href="assetsb/imgs/theme/favicon.svg">
    <!-- Template CSS -->
    <link href="assetsb/css/main.css" rel="stylesheet" type="text/css" />
</head>

<body>
    <div class="screen-overlay"></div>
    <%- include("../partials/aside")%>
    <main class="main-wrap">
        <%- include("../partials/header2")%>
        <section class="content-main">
            <form action="/admin/addCoupon" method="post" id="couponForm">
                <div class="row justify-content-center">
                    <div class="col-lg-12">
                        <h2 class="content-title" style="margin-left:300px;">Add New Coupon</h2>
                        <div class="card mb-4" style="margin-top:15px;margin-left: 135px;width: 70%;">
                            <div class="card-body">
                                <!-- Your form fields -->
                                <label for="couponCode" class="form-label">Coupon Code</label>
                                <input type="text" placeholder="Type here" id="couponCode" class="form-control" name="couponCode">
                                <div class="error"></div>
                                
                                <label for="discount" class="form-label">Discount (%)</label>
                                <input type="number" class="form-control" name="discount" id="discount">
                                <div class="error"></div>
                                
                                <label for="limit" class="form-label">Limit</label>
                                <input type="number" class="form-control" name="limit" id="limit">
                                <div class="error"></div>
                                
                                <label for="minCartAmt" class="form-label">Min Cart Amount</label>
                                <input type="text" placeholder="Type here" class="form-control" name="minCartAmt" id="minCartAmt">
                                <div class="error"></div>
                                
                                <label for="maxRedeemableAmt" class="form-label">Max Redeemable Amount</label>
                                <input type="text" placeholder="Type here" class="form-control" name="maxRedeemableAmt" id="maxRedeemableAmt">
                                <div class="error"></div>
                                
                                <label for="discountType" class="form-label">Discount Type</label><br>
                                <div class="form-check form-check-inline" style="margin-right: 40px;">
                                    <input class="form-check-input" type="radio" name="discountType" id="fixedDiscount" value="fixed" />
                                    <label class="form-check-label" for="fixedDiscount">Fixed</label>
                                </div>
                                <div class="form-check form-check-inline" style="margin-right: 40px;">
                                    <input class="form-check-input" type="radio" name="discountType" id="percentageDiscount" value="percentage" />
                                    <label class="form-check-label" for="percentageDiscount">Percentage</label>
                                </div>
                                <br>
                                
                                <label for="expiryDate" class="form-label">Expiry Date</label>
                                <input type="date" class="form-control" name="expiryDate" id="expiryDate">
                                <div class="error"></div>
                                
                                <div style="margin:  15px 0 12px 21px;">
                                    <input type="submit" class="btn btn-md rounded font-sm hover-up" value="Add Coupon">
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
            
            <script>
                // JavaScript code goes here
            </script>
            
            
            <script>
                // Validation script goes here (from the previous code snippet)
            </script>
            
       </section>
        <footer class="main-footer font-xs">
            <div class="row pb-30 pt-15">
                <div class="col-sm-6">
                    <script>
                    document.write(new Date().getFullYear())
                    </script> ©, Evara - HTML Ecommerce Template .
                </div>
                <div class="col-sm-6">
                    <div class="text-sm-end">
                        All rights reserved
                    </div>
                </div>
            </div>
        </footer>
    </main>
    <script src="assetsb/js/vendors/jquery-3.6.0.min.js"></script>
    <script src="assetsb/js/vendors/bootstrap.bundle.min.js"></script>
    <script src="assetsb/js/vendors/select2.min.js"></script>
    <script src="assetsb/js/vendors/perfect-scrollbar.js"></script>
    <script src="assetsb/js/vendors/jquery.fullscreen.min.js"></script>
    <script src="assetsb/js/vendors/chart.js"></script>
    <!-- Main Script -->
    <script src="assetsb/js/main.js" type="text/javascript"></script>
    <script src="assetsb/js/custom-chart.js" type="text/javascript"></script>
    <script>
        document.getElementById('couponForm').addEventListener('submit', function(event) {
            event.preventDefault(); // Prevent the form from submitting initially
                    
            // Validation for coupon form fields
            const couponCode = document.getElementById('couponCode');
            const discount = document.getElementById('discount');
            const limit = document.getElementById('limit');
            const minCartAmt = document.getElementById('minCartAmt');
            const maxRedeemableAmt = document.getElementById('maxRedeemableAmt');
            const expiryDate = document.getElementById('expiryDate');
            const discountType = document.querySelector('input[name="discountType"]:checked');
            
            clearErrors();
        
            let isValid = true;
        
            if (couponCode.value.trim() === '') {
                showError(couponCode, 'Please enter a coupon code');
                isValid = false;
            } else {
                clearError(couponCode);
            }
        
            if (discount.value.trim() === '' || isNaN(discount.value) || +discount.value < 0) {
                showError(discount, 'Please enter a valid discount');
                isValid = false;
            } else {
                clearError(discount);
            }
        
            if (limit.value.trim() === '' || isNaN(limit.value) || +limit.value < 0) {
                showError(limit, 'Please enter a valid limit');
                isValid = false;
            } else {
                clearError(limit);
            }
        
            if (minCartAmt.value.trim() === '' || isNaN(minCartAmt.value) || +minCartAmt.value < 0) {
                showError(minCartAmt, 'Please enter a valid minimum cart amount');
                isValid = false;
            } else {
                clearError(minCartAmt);
            }
        
            if (maxRedeemableAmt.value.trim() === '' || isNaN(maxRedeemableAmt.value) || +maxRedeemableAmt.value < 0) {
                showError(maxRedeemableAmt, 'Please enter a valid Max Redeemable Amount');
                isValid = false;
            } else {
                clearError(maxRedeemableAmt);
            }
        
            if (expiryDate.value.trim() === '') {
                showError(expiryDate, 'Please select an expiry date');
                isValid = false;
            } else {
                clearError(expiryDate);
            }
        
            if (discountType && discountType.value === 'percentage') {
                const discountValue = +discount.value;
                if (discountValue < 1 || discountValue > 100) {
                    showError(discount, 'Discount should be between 1 and 100 for percentage type');
                    isValid = false;
                } else {
                    clearError(discount);
                }
            }
            
            if (isValid) {
                this.submit();
            }
        });
        
        function showError(input, message) {
            const errorDiv = document.getElementById(input.id).nextElementSibling;
            errorDiv.innerText = message;
            errorDiv.style.color = 'red';
        }
        
        function clearError(input) {
            const errorDiv = document.getElementById(input.id).nextElementSibling;
            errorDiv.innerText = '';
        }
        
        function clearErrors() {
            const errorFields = document.querySelectorAll('.error');
            errorFields.forEach(function(errorField) {
                errorField.innerText = '';
            });
        }
    
        const formInputs = document.querySelectorAll('input[type="text"], input[type="number"], input[type="date"]');
        formInputs.forEach(input => {
            input.addEventListener('input', function() {
                clearError(this);
            });
        });
    
        const discountTypeInputs = document.querySelectorAll('input[name="discountType"]');
        discountTypeInputs.forEach(input => {
            input.addEventListener('change', function() {
                const discountInput = document.getElementById('discount');
                if (this.value === 'percentage') {
                    const discountValue = +discountInput.value;
                    if (discountValue < 1 || discountValue > 100) {
                        showError(discountInput, 'Discount should be between 1 and 100 for percentage type');
                    } else {
                        clearError(discountInput);
                    }
                } else {
                    clearError(discountInput);
                }
            });
        });
    </script>
    
</body>


<!-- Mirrored from wp.alithemes.com/html/evara/evara-backend/index.html by HTTrack Website Copier/3.x [XR&CO'2014], Sun, 01 Aug 2021 15:32:57 GMT -->
</html>



